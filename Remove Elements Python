class Solution(object):
    def removeElement(self, nums, val):
        """
        :type nums: List[int]
        :type val: int
        :rtype: int
        """
        while val in nums:
            target = nums.index(val)
            nums.pop(target)
        
        le = len(nums)
        return le
